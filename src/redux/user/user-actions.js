import { createAction } from '@reduxjs/toolkit';

export const addUser = createAction('add/User');
export const addNewUser = createAction('addNewUser/User');
export const addNewPassword = createAction('addNewPassword/User');
